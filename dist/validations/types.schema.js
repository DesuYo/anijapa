"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Joi = require("joi");
const STR = (length) => Joi.string().trim().max(length);
const NUM = (max) => Joi.number().max(max);
exports.VARCHAR = (length, def = null) => STR(length).default(def);
exports.$VARCHAR = (length) => STR(length).required();
exports.SLUG = (length, def = null) => STR(length).token().lowercase().default(def);
exports.$SLUG = (length) => STR(length).token().lowercase().required();
exports.URI = (length, def = null) => STR(length).uri({ allowRelative: true }).default(def);
exports.$URI = (length) => STR(length).uri({ allowRelative: true }).required();
exports.NAME = (length, def = null) => STR(length).regex(/^[a-zA-Z]$/).default(def);
exports.$NAME = (length) => STR(length).regex(/^[a-zA-Z]$/).required();
exports.EMAIL = (def = null) => STR(64).email().default(def);
exports.$EMAIL = () => STR(64).email().required();
exports.PHONE = (def = null) => STR(16).replace(/\(*\)*/g, '').regex(/^\+\d{11,12}$/).default(def);
exports.$PHONE = () => STR(16).replace(/\(*\)*/g, '').regex(/^\+\d{11,12}$/).required();
exports.INT = (max = Math.pow(2, 32), def = null) => NUM(max).integer().default(def);
exports.$INT = (max = Math.pow(2, 32)) => NUM(max).integer().required();
exports.UINT = (max = Math.pow(2, 32), def = null) => NUM(max).integer().positive().default(def);
exports.$UINT = (max = Math.pow(2, 32)) => NUM(max).integer().positive().required();
exports.FLOAT = (max = Math.pow(2, 32), precision, def = null) => NUM(max).precision(precision).default(def);
exports.$FLOAT = (max = Math.pow(2, 32), precision) => NUM(max).precision(precision).required();
exports.UFLOAT = (max = Math.pow(2, 32), precision, def = null) => NUM(max).precision(precision).positive().default(def);
exports.$UFLOAT = (max = Math.pow(2, 32), precision) => NUM(max).precision(precision).positive().required();
exports.BOOL = (def = false) => Joi.boolean().default(def);
exports.DATE = (def = null) => Joi.date().iso().default(def);
exports.$DATE = () => Joi.date().iso().required();
//# sourceMappingURL=types.schema.js.map